<!DOCTYPE html>
<html lang="en">
<head>
    <title>Movies | BookMyShow Clone</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }
        .navbar {
            background-color: #fff;
            padding: 15px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .navbar h2 {
            color: red;
        }
        .container {
            max-width: 1200px;
            margin: auto;
            padding: 20px;
        }
        .movies-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }
        .movie-card {
            width: 200px;
            background: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
            padding: 10px;
        }
        .movie-card img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-radius: 8px;
        }
        .movie-card h3 {
            margin: 10px 0;
        }
        .movie-card a {
            text-decoration: none;
            color: white;
            padding: 5px 10px;
            display: inline-block;
            border-radius: 5px;
            margin: 5px;
        }
        .favorite {
            background: red;
        }
        .delete {
            background: black;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <h2>BookMyShow Clone</h2>
        <a href="/movies/add">➕ Add Movie</a>
    </div>

    <div class="container">
        <h3>Default Movie Trailer</h3>
        <iframe width="100%" height="315" src="https://www.youtube.com/embed/gKizDojsdvs" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>

        <h2>Recommended Movies</h2>
        <div class="movies-grid">
            <% movies.forEach(movie => { %>
                <div class="movie-card">
                    <% if (movie.poster) { %>
                        <img src="<%= movie.poster %>" alt="<%= movie.title %>">
                    <% } else { %>
                        <img src="https://via.placeholder.com/200x250" alt="No Image">
                    <% } %>
                    <h3>Movie : <%= movie.title %></h3>
                    <p>Price : <%= movie.genre %></p>
                    <a href="/movies/favorite/<%= movie._id %>" class="favorite">
                        <% if (movie.isFavorite) { %>❤️ Favorite <% } else { %>⭐ Mark Favorite <% } %>
                    </a>
                    <a href="/movies/delete/<%= movie._id %>" class="delete">🗑 Delete</a>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
