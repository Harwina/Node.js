<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= todo ? "Edit Task" : "Add New Task" %></title>
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
        rel="stylesheet"
    >
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center"><%= todo ? "Edit Task" : "Add New Task" %></h1>
        <form action="<%= todo ? '/edittask/' + todo.id : '/addtask' %>" method="post" class="mt-4">
            <div class="mb-3">
                <label for="title" class="form-label">Task Title</label>
                <input
                    type="text"
                    id="title"
                    name="title"
                    class="form-control"
                    placeholder="Enter task title"
                    value="<%= todo ? todo.title : '' %>"
                    required
                >
            </div>
            <div class="mb-3">
                <label for="description" class="form-label">Task Description</label>
                <textarea
                    id="description"
                    name="description"
                    class="form-control"
                    placeholder="Enter task description"
                    rows="5"
                    required
                ><%= todo ? todo.description : '' %></textarea>
            </div>
            <button type="submit" class="btn btn-primary"><%= todo ? "Update Task" : "Add Task" %></button>
        </form>
        <div class="mt-3">
            <a href="/" class="btn btn-secondary">Back to Task List</a>
        </div>
    </div>
    <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
    ></script>
</body>
</html>
